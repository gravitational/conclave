package plan

import (
	"github.com/anthropics/conclave/internal/state"
)

// Plan is re-exported from state for convenience
type Plan = state.Plan

// Subsystem is re-exported from state for convenience
type Subsystem = state.Subsystem
